# Core API
fastapi>=0.110
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# Database
sqlalchemy>=2.0
alembic>=1.13

# LLM client (OpenRouter is OpenAI-compatible)
openai>=1.50
tenacity>=8.5
httpx>=0.27

# Observability (Arize Phoenix — behind a feature flag)
arize-phoenix-otel>=0.6
openinference-instrumentation-openai>=0.1

# Multi-agent debate (TinyTroupe — installed from git)
# pip install git+https://github.com/microsoft/TinyTroupe.git
# (kept out of this file so `pip install -r requirements.txt` works without git)

# Dev / utilities
python-multipart>=0.0.9
